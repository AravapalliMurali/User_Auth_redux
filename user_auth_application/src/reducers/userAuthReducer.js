const formInitaialValue = []

export default function userAuthReducer(state = formInitaialValue , action){
     switch(action.type){

        default :{
             return [...state]
        }
     }
}